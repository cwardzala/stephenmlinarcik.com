{
  "name": "stephenmliarcik.com",
  "version": "0.2.0",
  "description": "stephenmliarcik.com",
  "main": "index.js",
  "scripts": {
    "scss": "rm dist/css/*.css; node-sass --include-path node_modules --include-path src/scss --output-style compressed -o dist/css src/scss",
    "clean": "rm -rf dist; mkdir dist",
    "copy": "cp -R src/ dist",
    "build": "npm run clean; npm run copy; npm run scss"
  },
  "author": "Cameron Wardzala <cwardzala@findaway.com>",
  "license": "MIT",
  "devDependencies": {
    "node-sass": "^4.5.3"
  }
}
